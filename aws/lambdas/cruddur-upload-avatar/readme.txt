para crear el Gemfile, el cual es como el package.json o requirements.txt
```
bundle init
```

para correr la instalacion de las depenendencias
bundle install

para correr la funcion en ruby
bundle exec ruby function.rb


